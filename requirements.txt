# 核心依赖 (锁定版本以防冲突)
pandas>=2.0.0,<2.2.0
# 必须锁定 Numpy < 2.0.0，否则 baostock 会报错
numpy>=1.23.0,<2.0.0

# 数据源
baostock
requests

# 存储格式
pyarrow
zstandard

# 工具
tqdm

# 技术指标库
# 【关键修改】改为使用 zip 归档链接安装，避开 git clone 鉴权错误
https://github.com/twopirllc/pandas-ta/archive/development.zip
