# 核心依赖 (锁定版本以防冲突)
# Pandas 2.0+ 在 Python 3.10 下最稳定
pandas>=2.0.0,<2.2.0

# 【关键】必须锁定 Numpy < 2.0.0
# 这是解决 pandas-ta 安装失败的核心钥匙
numpy>=1.23.0,<2.0.0

# 数据源
baostock
requests

# 存储格式
pyarrow
zstandard

# 工具
tqdm

# 技术指标库
# 【回归 PyPI】
# 因为我们现在已经降级到了 Python 3.10 且锁定了 Numpy 版本，
# PyPI 上的标准版现在可以被正确识别和安装了。
pandas_ta
