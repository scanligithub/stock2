# 核心依赖 (锁定版本以防冲突)
# Pandas 2.0.x - 2.1.x 是目前兼顾性能和兼容性最好的版本
pandas>=2.0.0,<2.2.0

# 【关键】必须锁定 Numpy < 2.0.0
# 因为 baostock 和 pandas-ta 都不支持 Numpy 2.0
numpy>=1.23.0,<2.0.0

# 数据源
baostock
requests

# 存储格式
pyarrow
zstandard

# 工具
tqdm

# 技术指标库
# 【回归 PyPI】现在环境依赖已锁定，直接安装 PyPI 版本最稳妥
# 它是 Beta 版，但完全可用
pandas-ta
